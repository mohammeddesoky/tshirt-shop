# Leave empty to use relative paths (works with the dev proxy and the docker-compose nginx proxy).
# Set this only if your frontend and backend are on different domains, e.g.:
# VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_BASE_URL=
